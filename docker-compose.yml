services:
  memory:
    build: .
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - MEMORY_SECRET=${MEMORY_SECRET:-}
      - DB_PATH=/data/memories.db
