{
  "_comment": "Merge this into ~/.claude/settings.json (the user-level global config, NOT the project-level one).",
  "_comment2": "Replace YOUR-PI-HOSTNAME.tail1234.ts.net with your actual Tailscale Funnel URL from: tailscale status",

  "mcpServers": {
    "memory": {
      "type": "sse",
      "url": "https://YOUR-PI-HOSTNAME.tail1234.ts.net/mcp/sse",
      "headers": {
        "X-API-Key": "${MEMORY_API_KEY}"
      }
    }
  }
}
